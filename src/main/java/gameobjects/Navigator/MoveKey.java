package gameobjects.Navigator;

public enum MoveKey {

    MOVE_SUCCESSFUL("move successful"), BAD_COORDINATES("bad coordinates"),
    TILE_OCCUPIED("tile occupied"), LINK_TO_MAP("link to map");
    MoveKey(String s) {
    }
}
